# HLB-현장지원팀 CRM 오류 검증 및 기능 테스트 보고서

## 최신 검증 · 사용자별 조회 범위와 내 계정 창 (2026-09-11)

- 최종 `npm test`: **108건 통과, 실패 0건** (회귀 방지 2건 추가, 세 번 연속 동일 결과)

### 1. 변경 이력·휴지통을 사용자별로 나눴습니다

| 상태 | 변경 이력 | 휴지통 |
|---|---|---|
| 로그인한 사용자 | **자기가 남긴 기록만** (관리자 인증 불필요) | **자기가 지운 항목만** |
| 관리자 인증 | 전체 · 사용자별로 좁혀 보기 | 전체 · 누가 지웠는지 함께 표시 |
| 사용자 계정 미사용 | 관리자 인증 필요(기존과 동일) | 전체(기존과 동일) |

- **서버에서 거릅니다.** `/api/audit`는 관리자 세션이 없으면 조회 조건을 로그인한 사용자의 기록으로 **강제 고정**합니다. 화면 목록·사용자 목록도 그 범위 안에서만 집계해 다른 사람 이름이 노출되지 않습니다.
- 휴지통 항목에 **지운 사람**(`deletedBy`)을 남기고, 목록·접기 개수·비우기가 모두 같은 범위를 따릅니다. `비우기`는 지금 보이는 항목만 비우므로 남의 항목을 지우지 않습니다.
- 지운 사람 기록이 없는 예전 항목은 복구할 길이 막히지 않도록 모두에게 보입니다.
- 이전에는 관리자만 볼 수 있던 화면이라, 일반 사용자도 로그인만 하면 변경 이력 패널이 열립니다.

### 2. 이름을 누르면 내 계정 창이 열립니다

오른쪽 위 `강민지 편집자`를 누르면 **내 계정** 창이 뜨고 두 가지를 할 수 있습니다.

- **비밀번호 변경** — 기존 비밀번호와 새 비밀번호(6자 이상, 확인 입력 포함)를 넣어 직접 바꿉니다. 관리자 코드 없이 자기 계정만 바꿀 수 있고, **기존 비밀번호는 서버에서** 대조합니다. 비밀번호를 정한 적이 없으면 기존 칸을 비워 두고 새로 정합니다.
- **로그아웃** — 로그인 정보를 지우고 **처음 주소로 접속한 것과 같은 상태**로 돌아갑니다. 보던 화면 주소(`#해시`)까지 지워 사용자 로그인 화면부터 시작하므로, 앞사람의 관리자 인증·편집 내용·AI 대화가 넘어가지 않습니다.

이전에는 이름을 누르면 곧바로 로그아웃되었습니다.

### 검증

- 회귀 방지 테스트 2건 추가 (`test/fullstack.test.js`)
  - **"사용자는 자기 변경 이력·휴지통만 보고, 관리자 인증을 하면 전체가 보인다"** — 갑 사용자가 연락처를 만들고 지운 뒤, 갑 화면에서는 자기 기록만 보이고, 을 사용자로 바꾸면 갑의 기록도 휴지통 항목도 사라지며, 관리자 인증을 하면 전체와 사용자 선택이 나타나는 것까지 한 번에 지납니다.
  - **"내 계정 창에서 기존 비밀번호를 확인하고 새 비밀번호로 바꾼다"** — 기존 비밀번호가 틀리면 바뀌지 않고, 확인 입력이 다르면 서버로 보내지도 않으며, 제대로 넣으면 **새 비밀번호로 로그인되고 예전 비밀번호는 막히는 것**을 서버 응답으로 확인합니다.
- `test/api.test.js`도 `/api/audit`의 권한 규칙과 비밀번호 변경 경로를 확인하도록 바꿨습니다.
- 실제 브라우저에서 강민지(비밀번호 없음)·박대리(비밀번호 있음) 두 계정으로 전 과정을 확인했습니다. 관리자 인증 시 전체 30건과 사용자 선택(`강민지 (3)`)이 나타나고, 비밀번호 변경 후 새 비밀번호 로그인 200 / 예전 비밀번호 403, 로그아웃 후 주소가 `/#settings` → `/`로 돌아가며 로그인 화면이 뜨는 것을 확인했습니다.

### 참고

- 첨부해 주신 메뉴 줄바꿈 화면은 자산 버전 `20260911h` 이전 상태입니다. 줄바꿈 수정은 `20260911i`에 들어갔고, 지금은 `20260911k`입니다. 배포 후 새로고침하면 한 줄로 나옵니다.

---

## 수정 3건: 사용자 전환 초기화 · 메뉴 줄바꿈 · 목록 저장 버튼 (2026-09-11)

- 최종 `npm test`: **106건 통과, 실패 0건** (회귀 방지 3건 추가, 세 번 연속 동일 결과)

### 1. 사용자를 바꿔도 앞사람의 관리자 인증이 남던 문제

**증상** — 관리자 백업·복원을 활성화한 뒤 사용자를 바꾸면 다음 사람 화면에서도 **여전히 활성화된 상태**였습니다.

**원인** — 사용자 전환은 로그인 정보만 비우고, 그 브라우저 화면에 남아 있던 나머지 상태는 그대로 두었습니다. 관리자 인증 토큰, 관리자가 불러온 사용자 목록과 변경 이력, 편집 중이던 내용, AI 대화가 모두 이어졌습니다.

**수정** — 사용자를 바꿀 때 **화면을 처음부터 다시 엽니다.** 빠뜨리는 항목이 생기지 않도록 하나씩 지우는 대신 화면 자체를 새로 시작합니다. 새로 열기 전에 관리자 인증을 먼저 해제하고, 저장하지 않은 변경이 있으면 사라진다는 것을 먼저 확인받습니다.

| 항목 | 이전 | 이후 |
|---|---|---|
| 관리자 백업·복원 | 활성화된 채로 유지 | **잠김 (활성화 버튼으로 복귀)** |
| 관리자가 불러온 사용자 목록·변경 이력 | 화면에 남음 | 비워짐 |
| 편집 중이던 내용 | 다음 사람에게 넘어감 | 확인 후 정리 |
| AI 대화 기록 | 남음 | 비워짐 |
| 권한(편집/열람) | 전환 전 상태가 남을 수 있음 | 새 사용자 권한으로 다시 적용 |

**같이 고친 것** — 로그인 화면이 여러 번 열리면 사용자 목록을 그때마다 새로 불러오면서 **고르던 이름이 지워지는** 경합이 있었습니다. 목록 요청을 한 번만 보내 함께 쓰고, 목록이 그대로면 다시 그리지 않으며, 불러오기에 실패해도 이미 떠 있는 목록은 지우지 않습니다. 목록이 도착하기 전에 로그인을 눌러도 기다렸다가 진행합니다.

> 참고: 단계·그룹·분류 같은 **팀 공용 설정**과 초보 모드·대시보드 순서·페이지 줄 수 같은 **사용 환경**은 지금도 작업공간에 공유됩니다(모든 사용자가 같은 값을 봅니다). 사람마다 다르게 두려면 별도 작업이 필요합니다.

### 2. 상단 메뉴 글씨가 두 줄로 접히던 문제

**증상** — `대시 보드`, `파이 프라 인`, `연 락 처`처럼 메뉴 글씨가 두 줄로 접혔습니다.

**원인** — 상단 바는 줄바꿈을 하지 않는 대신 **탭을 줄여서** 공간을 맞췄고, 좁아진 탭 안에서 글씨가 접혔습니다. 활동 메뉴가 늘고 사용자 이름 칸이 생기면서 노트북 폭(1,200px 안팎)부터 나타났습니다.

**수정** — 탭 글씨를 한 줄로 고정하고(`white-space:nowrap`) 탭이 줄어들지 않게 했습니다. 대신 폭이 모자라면 **메뉴 줄만 가로로 밀어** 어떤 메뉴도 잘리지 않게 하고, 1,320px·1,160px·960px 구간에서 여백·글자·로고를 단계적으로 줄여 대부분의 화면에서는 밀지 않아도 들어가게 했습니다.

| 폭 | 이전 | 이후 |
|---|---|---|
| 1,100px | 7개 모두 두 줄 | **전부 한 줄, 밀림 없음** |
| 980px | 두 줄 | **전부 한 줄, 밀림 없음** |
| 820px | 두 줄 | 전부 한 줄 (메뉴 줄만 가로로 밀림) |

### 3. 파이프라인 목록의 저장 버튼이 카드 밖으로 나가던 문제

**증상** — 목록 보기에서 행의 `저장`·`×` 버튼이 둥근 카드 테두리 바깥으로 밀려 나왔습니다.

**원인** — 마지막 칸의 폭이 **52px**로 지정되어 있는데 버튼 두 개는 90px 이상이 필요했습니다. 표가 지정한 폭을 그대로 쓰는 방식(`table-layout:fixed`)이라 넘치는 만큼 카드 밖으로 나갔습니다.

**수정** — 마지막 칸을 **132px**로 넓히고(파이프라인 목록과 그룹 상세 표 모두), 버튼을 카드 오른쪽 안쪽에 붙였습니다. 실제 브라우저에서 버튼 오른쪽 끝이 카드 테두리보다 13px 안쪽에 들어오는 것을 확인했습니다.

### 검증

- 회귀 방지 테스트 3건 추가
  - `test/fullstack.test.js` — **"사용자를 바꾸면 앞사람의 관리자 인증과 편집 내용이 넘어가지 않는다"**: 로그인 → 관리자 인증 → 사용자·변경 이력 조회까지 한 뒤 사용자를 바꾸고, 인증·목록·이력·AI 대화·로그인 정보가 모두 비워졌는지 확인합니다.
  - `test/ui.test.js` — 메뉴 글씨 한 줄 고정과 가로 밀림 규칙, 파이프라인 마지막 칸 폭과 버튼 정렬을 확인합니다.
  - 세 테스트 모두 **수정을 되돌리면 실패하는 것**을 확인한 뒤 되살렸습니다.
- 실제 브라우저에서 1,100 / 980 / 820px 폭의 메뉴, 목록 저장 버튼 위치, 사용자 전환 후 관리자 인증 해제를 직접 확인했습니다.

---

## 버그 수정: 접속 직후 뜬 로그인 화면에서 로그인이 되지 않던 문제 (2026-09-11)

- 최종 `npm test`: **103건 통과, 실패 0건** (재발 방지 테스트 1건 추가, 세 번 연속 동일 결과)

### 증상

사용자를 추가한 뒤 CRM을 열면 `사용자 확인` 화면이 뜨는데, **로그인 버튼을 눌러도 아무 일이 일어나지 않고 화면이 닫히지 않았습니다.**

### 원인

비밀번호와는 무관한 **초기화 순서 문제**였습니다.

`init()`은 접속 확인 다음에 `await ensureMemberSession()`으로 로그인이 끝나기를 기다리는데, 로그인 폼의 제출 처리를 연결하는 코드가 그보다 **한참 아래**에 있었습니다. 그래서 로그인 화면에서 기다리는 동안에는 버튼에 아무 동작도 붙어 있지 않았습니다.

- 서버는 정상이었습니다. 비밀번호를 비워 둔 사용자로 `POST /api/auth/login`을 직접 호출하면 200과 토큰이 정상 발급되는 것을 확인했습니다.
- 저장하다가 뜬 로그인 화면(이미 초기화가 끝난 뒤)은 정상 동작했기 때문에, 첫 확인에서는 드러나지 않았습니다.

### 수정

1. **로그인 화면의 버튼을 먼저 연결합니다.** 접속키 화면(`setupCloudStorageUi`)과 같은 자리에서 `setupMemberGateUi()`를 호출해, `init()`이 기다리기 전에 제출·취소·사용자 전환을 모두 연결합니다.
2. **고를 이름이 없으면 갇히지 않습니다.** 관리자가 사용자를 모두 지우거나 중지하면 서버도 로그인을 요구하지 않으므로, 목록을 받아 비어 있으면 로그인 화면을 스스로 닫고 진행합니다. 목록을 못 받은 경우(네트워크 오류)에는 닫지 않고 사유를 화면에 적습니다.

### 검증

- 재발 방지 테스트 추가 — `test/fullstack.test.js`의 **"사용자가 있으면 접속 직후 뜬 로그인 화면에서 바로 로그인된다"**. 비밀번호를 비워 둔 사용자를 만들고 **새 화면을 처음부터 띄워** 로그인 버튼을 실제로 누릅니다(폼 제출 이벤트를 직접 만들지 않습니다).
- 이 테스트가 실제로 버그를 잡는지 확인하기 위해 **수정을 잠시 되돌려 다시 돌렸고, `시간 초과: 로그인 후 사용자 확인 화면 닫힘`으로 실패**하는 것을 확인한 뒤 되살렸습니다.
- 실제 브라우저에서도 같은 순서로 확인했습니다. 접속 → 로그인 화면 → 비밀번호를 비운 채 버튼 클릭 → 로그인 완료 → 화면 시작 → 연락처 저장까지 성공하고, 로그인 화면이 다시 뜨지 않았습니다.
- 사용자를 모두 지운 뒤 로그인 화면을 강제로 띄우면 스스로 닫히고 접속키만으로 쓰는 상태로 돌아가는 것도 확인했습니다.

---

## 기능 7건 추가 — 변경 이력·활동·전체 검색·사용자 권한·중복 정리·파이프라인 Excel·AI 확장 (2026-09-11)

- 최종 `npm test`: **102건 통과, 실패 0건** (신규 12건 추가, 세 번 연속 동일 결과)
- 실제 화면(jsdom) → 실제 Express → 실제 PostgreSQL(PGlite) 경로와 실제 브라우저 양쪽에서 확인했습니다.

### 추가한 기능

| # | 기능 | 위치 |
|---|---|---|
| 1 | **변경 이력 조회** — 화면·동작·사용자·기간·검색어로 좁혀 보고, 상세에서 이전 값 → 이후 값 비교 | 설정 > 변경 이력 |
| 2 | **활동 화면** — 기간별 활동 타임라인, 유형별 집계, 접촉이 끊긴 영업건, CSV 내보내기 | 새 메뉴 `활동` |
| 3 | **전체 검색** — Ctrl/⌘+K로 영업·연락처·지원 업무·일정·매뉴얼을 한 번에 검색 | 상단 바 |
| 4 | **사용자 계정·권한** — 관리자/편집자/열람자, scrypt 비밀번호, 서버에서 쓰기 차단, 변경 이력에 작업자 기록 | 설정 > 사용자 + 로그인 화면 |
| 7 | **중복 연락처 정리** — 이름·연락처·이메일이 겹치는 연락처를 묶어 대표를 고르고 빈 칸만 채워 병합 | 연락처 > 중복 정리 |
| 11 | **파이프라인 Excel** — 지금 보이는 목록을 내보내고, 고친 표를 그대로 가져와 수정·신규 반영 | 파이프라인 도구 |
| 14 | **AI 확장** — 활동 요약 초안 작성, 단계·마지막 접촉 기준 다음 액션 추천 | 활동 모달 · 항목 상세 |

### 사용자 계정을 서버에서 강제하는 방식

- 접속키(`CRM_ACCESS_KEY`)는 그대로 두고, 그 안쪽에 사용자 로그인을 한 겹 더 두었습니다. **사용자를 한 명도 만들지 않은 작업공간은 예전과 완전히 동일하게 동작합니다.**
- 비밀번호는 scrypt 해시와 임의 소금으로만 `crm_member` 테이블에 저장합니다. 원문도, 되돌릴 수 있는 형태도 남기지 않습니다.
- 로그인 토큰은 접속키로 서명한 HMAC입니다. 서명 위조·만료·다른 작업공간·권한만 바꿔치기한 토큰을 모두 거부하는 것을 자동 테스트로 확인했습니다.
- 토큰은 "누구인지"만 증명하고, **권한과 사용 여부는 매 저장마다 최신 사용자 목록에서 다시 읽습니다**(30초 캐시). 권한을 내리거나 사용자를 지우면 이미 발급된 토큰으로도 저장할 수 없습니다.
- 열람자의 저장 요청은 화면에서 버튼을 막는 것과 별개로 **서버가 403으로 거부**합니다. 화면 차단만으로는 막히지 않는다는 점을 실제 요청으로 검증했습니다.

### 검증

- 자동 테스트 102건 통과. 새로 추가한 12건:
  - `test/members.test.js` 4건 — 비밀번호 해시·대조, 토큰 위조/만료/작업공간 검사, 권한 판정, 입력값 검사
  - `test/api.test.js` 2건 — 새 API가 접속키와 관리자 인증을 모두 요구하는지, 사용자가 없을 때 예전 동작이 유지되는지
  - `test/fullstack.test.js` 6건 — 전체 검색, 활동+AI 초안·추천, 중복 병합, 파이프라인 Excel 왕복, 사용자 권한 전체 흐름, 변경 이력 조회·필터·상세
- 사용자 권한 테스트는 실제로 **사용자 추가 → 로그인 없는 저장 403 → 화면 로그인 → 저장 성공 → 변경 이력에 이름 기록 → 열람자로 변경 후 저장 차단 → 사용자 삭제 후 원래대로 복구**까지 한 번에 지납니다.
- 실제 브라우저에서도 같은 흐름을 확인했습니다. 저장을 누르면 로그인 화면이 뜨고, 로그인하면 **중단됐던 저장이 자동으로 이어져** 성공하며, 변경 이력에 `김현장`이 남았습니다.

### 이 과정에서 고친 것

1. **새 오버레이가 스타일을 받지 못하던 문제** — 전체 검색·사용자 편집·변경 이력 상세·중복 정리 창을 `#tn-root` 바깥에 넣어 두어, `#tn-root` 기준으로 작성된 스타일이 하나도 적용되지 않았습니다. 브라우저 확인에서 발견해 모두 안쪽으로 옮겼습니다.
2. **조회 중 조건을 바꾸면 요청이 사라지던 문제** — 변경 이력에서 앞선 조회가 끝나기 전에 화면·기간을 바꾸면 그 요청이 조용히 버려졌습니다. 진행 중이면 표시해 두었다가 끝난 뒤 한 번 더 돌리도록 바꿨습니다.
3. **관리자 인증 직후 자동 조회의 오류 알림** — 사용자·변경 이력을 미리 당겨 오는 요청이 실패하면 토스트가 떴습니다. 자동 조회는 조용히 두고 패널 안에 사유만 적으며, `새로고침`을 눌렀을 때만 알립니다.
4. **변경 이력 요약이 읽히지 않던 문제** — 새로 만든 항목까지 바뀐 필드를 모두 늘어놓아 "주소, birthday, businessPhone 외 22개"처럼 보였습니다. 입력·삭제·복원은 요약 문장을, 수정만 필드 목록을 보여 주도록 바꿨습니다.
5. **변경 이력에 영문 키가 그대로 보이던 문제** — 휴대전화·회사 전화·직책·부서·명함 등 연락처와 영업 항목의 이름 30여 개를 한글 이름표에 추가했습니다.

### 남은 확인 사항

- 변경 이력의 이름표는 **기록될 때 함께 저장**됩니다. 이번 변경 이전에 쌓인 기록은 예전 표기 그대로 남습니다.
- 사용자 계정을 켜기 전에 `render.txt`의 평문 접속키를 지우고 `CRM_ACCESS_KEY`를 교체하세요. 접속키는 로그인 토큰 서명에도 쓰이므로, 교체하면 모든 사용자가 다시 로그인합니다.

### 실행 명령

~~~bash
npm ci
npm test
~~~

---

## 이전 검증: 접속 비용·화면 속도·첫 로딩 최적화 6건 (2026-09-11)

- 최종 `npm test`: **90건 통과, 실패 0건** (지연 로딩 확인 테스트 1건 추가)
- 실제 화면 → 실제 Express → 실제 PostgreSQL(PGlite)로 접속 요청 수·렌더 시간·파일 크기를 최적화 전후로 측정했다.

### 측정 결과

| 항목 | 이전 | 이후 | 변화 |
|---|---|---|---|
| 첫 접속 요청 수 | 62건 (조회 39 · **저장 23**) | 41건 (조회 40 · **저장 1**) | −34% |
| 재접속 요청 수 | 32건 | **20건** | −38% |
| 재접속 시 마이그레이션 조회 | 14건 | **2건** | −86% |
| 파이프라인 목록 렌더 (200건) | 138ms | **17ms** | −88% |
| 대시보드 렌더 | 68ms | **11ms** | −84% |
| 연락처 렌더 | 13ms | 10ms | −23% |
| 첫 로드 JavaScript | 493KB (gzip 135KB) | **441KB (gzip 118KB)** | −13% |
| 정적 파일 캐시 | 1시간 | **1년(재확인 없음)** | 재방문 시 재다운로드 없음 |

파이프라인 **보드** 화면(112ms → 97ms)은 이번 범위에 넣지 않았다. 기본 화면은 목록이며, 보드를 페이지로 나누면 단계별 카드 흐름을 한눈에 보는 목적이 흐려지기 때문이다.

### 수행한 최적화

1. **매뉴얼 마이그레이션 기록을 키 하나로 통합** — 접속할 때마다 13개 키를 따로 조회하던 것을 `tinico:manual:migrations` 한 곳에 모았다. 예전 방식으로 저장된 작업공간은 통합 기록이 없을 때 한 번만 13개를 읽어 합치고, 그 뒤로는 1건만 조회한다.
2. **파이프라인 목록 페이지 나누기** — 항목이 200건이면 표 행 200개를 매번 새로 만들었다. 연락처와 같은 방식(기본 20줄, 20·30·40·50 선택)으로 바꿔 화면에 보이는 만큼만 만든다. 고른 줄 수는 외부 DB에 저장되어 다음 접속에도 유지된다. 검색·필터를 바꾸면 1페이지로 돌아간다.
3. **명함 인식·Excel 백업 코드를 `public/heavy.js`로 분리** — 첫 화면에 필요하지 않은 61KB를 떼어 내 실제로 그 기능을 쓸 때만 내려받는다. 이미 불러와 있으면 기다리지 않고 바로 실행한다. 불러오지 못하면 사용자에게 알린다.
4. **첫 접속 저장을 한 번의 트랜잭션으로** — 기본 그룹·매뉴얼·단계별 항목함을 키마다 따로 저장하던 23번의 왕복을 11개 키를 담은 **1회 저장**으로 합쳤다. 중간에 끊겨 일부만 저장되는 상황도 함께 사라졌다.
5. **대시보드 렌더 비용 절감** — 화면을 다시 그릴 때마다 속성 선택자로 문서 전체를 훑던 접기/펼치기 처리를 찾은 요소를 기억하도록 바꾸고, 순서가 이미 맞으면 DOM을 옮기지 않게 했다.
6. **정적 파일 캐시 정책** — `?v=` 로 버전이 붙은 요청은 1년간 재확인 없이 쓰도록 하고, 버전 없이 직접 연 주소는 기존처럼 1시간만 캐시한다. `index.html`은 항상 새로 확인한다.

### 검증

- 자동 테스트 90건 통과. 지연 로딩이 실제로 동작하는지 확인하는 테스트를 추가했다(첫 화면에는 `openScanner`가 없고, 불러온 뒤에는 Excel 파일 생성·복원까지 동작하며, 두 번 호출해도 한 번만 내려받는다).
- 실제 브라우저에서 확인: 첫 화면 스크립트는 `app.js`·`styles.css`뿐이고 `heavy.js`는 명함 업로드를 누른 뒤에 받아 온다. 파이프라인 63건이 20줄씩 4쪽으로 나뉘고, 다음·마지막 이동과 50줄 변경이 DB에 저장된다.
- 캐시 헤더는 `?v=` 유무에 따라 `max-age=31536000, immutable` / `max-age=3600` 으로 나뉘고 `index.html`은 `no-cache`임을 확인했다.

### 실행 명령

~~~bash
npm ci
npm test
~~~

---

## 이전 검증: 기능별 저장·수정·삭제 전수 점검 (2026-09-11)

- 최종 `npm test`: **89건 통과, 실패 0건** (회귀 테스트 17건 추가)
- 화면(jsdom) → 실제 Express → 실제 PostgreSQL(PGlite)을 그대로 이어 붙인 `test/fullstack.test.js`를 추가해, API를 흉내 내지 않고 운영과 같은 경로로 기능별 저장·수정·삭제를 확인했다.

### 발견해 수정한 문제

1. **설정 저장이 원자적이지 않았다.** 단계·분류·그룹·영업 항목은 서로를 참조하는데, `단계 삭제`·`단계 이름 변경`·`분류 저장/삭제`·`그룹 저장`·`중요도 저장`은 저장을 2~N회로 나눠 보냈다. 중간에 한 번이라도 실패하면 **설정에는 없는 단계·분류를 영업 항목이 가리키는 상태**가 DB에 남았다. 실제로 중간 실패를 주입해 재현했다.
   - 서로 얽힌 키를 **한 번의 트랜잭션**(`saveSettingsTogether`)으로 저장하도록 바꿨다. 서버가 전부 적용하거나 전부 취소한다.
2. **같은 여섯 곳에 오류 처리가 없었다.** 저장이 실패해도 화면 값은 이미 바뀐 뒤라 되돌아가지 않았고, 예외가 그대로 빠져나가 사용자에게 원인이 전달되지 않았다.
   - 실패 시 **바꾸기 전 값으로 되돌리고** 안내하도록 했다(`settingsSnapshot` / `restoreSettingsSnapshot`).
3. **화면 값만 바뀌고 DB와 어긋나던 동작 8곳**을 같은 방식으로 보강했다(`saveOrRollback`).
   - 대시보드 `오늘 할 일`의 두 `일정 변경` 버튼 (마감일·다음 연락일)
   - 지원 업무 목록의 상태 바로 바꾸기
   - 매뉴얼 추가·수정·삭제·순서 이동·기본값 초기화
   - AI 담당자 삭제
   - Google 클라이언트 ID 저장(일정과 앱 설정을 함께 저장)
   - 기본 예시 항목 정리, 사용 안내 닫기
4. **죽은 코드 11개 함수를 제거**했다. 그중 `syncLinkedDealsFromContact`는 동시 저장 개편 때 서버(`src/db.js`)로 옮겨졌는데 클라이언트에 남아 있었다. 서버 쪽 반영이 실제로 동작하는지 별도 테스트로 확인한 뒤 지웠다.

### 기능별 확인 결과 (실제 DB 기준)

| 기능 | 확인한 흐름 | 결과 |
|---|---|---|
| 접속 | 연결키 인증, 기본 그룹·매뉴얼 최초 저장 | 통과 |
| 연락처 | 추가 → 저장 → 수정 → 휴지통 → 복구 → 영구 삭제, 변경 로그 기록 | 통과 |
| 파이프라인 | 추가 → 저장 → 단계 변경 → 휴지통 → 복구 | 통과 |
| 지원 업무 | 추가 → 목록에서 상태 변경(진행률 자동 100) → 수정 → 삭제 | 통과 |
| 캘린더 | 일정 추가 → 수정 → 삭제 | 통과 |
| 설정 | 그룹·단계·분류 추가와 삭제, 매뉴얼 CRUD, 사용 환경 저장 | 통과 |
| 연락처 ↔ 영업 | 연락처 수정이 연결된 영업 항목의 담당자 이름·연락처에 반영 | 통과 |
| 전환 | 영업 항목 → 지원 업무 전환 시 양쪽 동시 반영 | 통과 |
| 휴지통 | 이동·복구·영구 삭제·비우기 | 통과 |
| 백업·복원 | Excel 내려받기 → 데이터 변경 → 복원으로 시점 복귀 | 통과 |
| 저장 실패 | 설정·지원 업무·매뉴얼·대시보드에서 되돌리기와 안내 | 통과 |

### 접속 비용 계측 (실제 서버·DB)

| 상황 | 요청 수 |
|---|---|
| 첫 접속(빈 작업공간) | 62건 (조회 39 · 저장 23) |
| 재접속(앱이 저장한 상태) | **32건 (조회 32 · 저장 0)** |

재접속 시 저장 왕복이 0건인 것은 이전 최적화가 유지되고 있음을 뜻한다.

### 화면 렌더 비용 (연락처 200건 · 영업 200건)

| 화면 | 시간 |
|---|---|
| 파이프라인 목록 | 85ms |
| 파이프라인 보드 | 63ms |
| 대시보드 | 40ms |
| 연락처 | 7ms |
| 지원 업무 / 설정 | 3ms |

### 확인하지 않은 범위

운영 Render 배포, 운영 Neon 데이터베이스, 실제 Google 계정 연동, 물리 휴대폰 카메라는 이번 검증에 포함하지 않았다.

### 실행 명령

~~~bash
npm ci
npm test
~~~

---

## 이전 검증: 항목별 동시 저장 보호 (2026-09-11)

- 최종 `npm test`: **72건 통과, 실패 0건**. 앱·API·DB·동시 저장 모듈·서버 JavaScript 문법 검사 통과.
- PostgreSQL WASM 엔진(PGlite)에서 운영 `schema.sql`, `createPostgresRepository`, Express API를 실행하여 9건 검증: 기존 배열 이전, 서로 다른 항목의 동시 요청, 같은 항목 충돌, 신규 ID 경쟁, 중복 요청 재사용, 삭제·재생성, 연락처·명함·연결 영업의 원자적 저장, 복원 후 세대 보호, 버전 없는 요청 차단.
- PGlite는 단일 DB 연결이며 테스트 풀에서 트랜잭션별로 연결을 대여한다. 운영 Neon의 여러 연결 부하 시험을 수행한 것은 아니다. 운영 구현은 짧은 작업공간 advisory lock으로 신규 생성·일괄 저장·복원을 직렬화하고 항목별 버전을 검사한다.
- UI에서는 입력 유지와 최신 값 선택, 재저장 직전의 추가 충돌, 다른 항목 저장에 의한 데이터 손실 방지, 삭제 충돌 시 휴지통 미생성, 복원 후 이전 입력 유지, 성공 응답 유실 후 같은 요청 재시도와 새 캘린더 일정의 수동 재시도 중복 방지를 검증했다.
- 파이프라인·연락처의 명시적 저장 버튼, 다중 담당자 콤보박스·대표 선택, 캘린더 담당자·업무 연결, 리멤버 CSV, Excel/JSON 백업·복원, 관리자 활성화, 매뉴얼 재접속 회귀 테스트 통과.
- 연락처와 연관 정보, 휴지통 이동·복구, 그룹 삭제, 영업→지원 업무 전환을 트랜잭션으로 묶었다. 복원은 세대 번호를 변경하며 삭제한 ID의 버전 기록은 유지한다. 성공 요청은 7일 보관 후 정리한다.
- 사용자 매뉴얼 및 README를 갱신했고 브라우저 자산 버전을 `20260911a`로 변경했다. 기존 백업 형식은 유지한다.
- 운영 Render/Neon/Google 계정에는 접근하지 않았으며 배포·운영 DB 변경은 수행하지 않았다. 서버 재시작 시 테이블이 추가되고 기존 데이터의 항목 버전은 최초 조회 시 준비된다.

---

## 이전 검증: 관리자 백업·복원 활성화 버튼 (2026-09-10)

- 설정 우측 상단의 관리자 버튼을 제거하고 ‘관리자 백업·복원’ 제목 옆에 ‘활성화’ 버튼을 배치했다.
- 관리자 코드 확인 후 ‘활성화됨’으로 표시하고 내려받기·복원하기가 열린다. 재클릭 시 인증 갱신, 만료 시 비활성 상태로 돌아가는 기존 인증 흐름을 유지한다.
- 잠금 안내, 기본·저장 매뉴얼과 README의 버튼 위치 안내를 갱신했다.
- 관련 회귀 테스트 **6건 통과**: 인증 실패·성공, 전체 정적 버튼 연결, 재접속 시 불필요한 저장 방지, Excel 백업 내려받기·복원 및 이전 JSON 복원.
- 동시 수정 처리의 기존 버전 검사와 저장 단위를 검토했다. 동시 수정 구조 변경은 이번 수정에 포함하지 않았다.

---

## 이전 검증: HLB 테마·담당자 예시·매뉴얼 갱신 (2026-09-10)

- 화면 구성을 유지하면서 HLB 로고의 `#EF4036`, 짙은 붉은색 버튼, 차콜·흰색과 Pretendard의 굵기·자간을 적용했다. 우측 상단 ‘간편 모드’ 표시를 제거했다.
- 파이프라인 내부 담당자와 지원 업무 담당자의 입력 예시를 `홍길동`으로 변경했다.
- 기본 매뉴얼 13개 섹션의 관련 내용을 최신화했다. 현재 메뉴 순서·색상, 저장/취소, 분석 목록, 담당자 선택, 일정 연관 업무, Excel 백업/복원과 휴지통 접기를 반영했다. 기존 저장 매뉴얼은 알려진 과거 안내만 교체하고 추가 작성 내용은 유지한다.
- 기본 매뉴얼 복원에도 최신 내용이 적용되며, 재접속 시 안내 중복이나 불필요한 저장이 생기지 않는지 확인했다.
- 최종 `npm test`: **58건 통과, 실패 0건**. JavaScript 문법 검사 통과.
- 실제 로컬 브라우저에서 테마, 상단 표시 제거, `예: 홍길동`, 최신 매뉴얼 렌더링을 확인했다. 운영 배포는 수행하지 않았다.

---

## 이전 검증: Excel(.xlsx) 백업·복원과 휴지통 접기 (2026-09-10)

- 결과: 자동 테스트 58건 통과, 실패 0건 (회귀 테스트 3건 추가)
- **실제 Excel로 파일을 열어 3개 시트와 내용이 정상 표시되는 것을 확인했습니다.**

## 변경 내용

1. 관리자 `내려받기`가 `hlb_busisup_crm_backup_년-월-일_시-분-초.xlsx` 파일을 만듭니다. Excel에서 바로 열 수 있는 통합 문서이며 시트 세 장으로 구성됩니다.
   - `백업정보` — 형식·버전·작업공간·내려받은 시각과 데이터·로그 건수, 화면별·동작별 합계
   - `저장데이터` — 저장키 · 리비전 · 수정시각 · 값(JSON) · 조각
   - `변경로그` — 이벤트 ID · 시각 · 화면 · 동작 · 데이터 종류 · 이름 · ID · 저장키 · 변경 요약 · 상세(JSON) · 조각
2. `복원하기`가 `.xlsx`를 읽습니다. 예전에 내려받은 `.json` 백업과 기존 단일 HTML의 버전 2 백업도 그대로 받습니다. 파일 앞 2바이트로 형식을 구분합니다.
3. Excel 한 칸 상한(32,767자)을 넘는 긴 값은 `조각` 번호를 붙여 여러 줄로 나눠 담고, 복원할 때 순서대로 다시 합칩니다. 명함 이미지처럼 큰 값도 손실 없이 오갑니다.
4. 휴지통 목록에 `접기/펼치기`를 추가했습니다. 접으면 버튼에 남은 항목 수(`펼치기 · 3개`)가 표시되고, 접은 상태는 앱 설정으로 외부 DB에 저장되어 다음 접속에도 유지됩니다. 저장에 실패하면 이전 상태로 되돌리고 알립니다.
5. 설정 > 매뉴얼의 설정·운영 안내에 Excel 백업과 휴지통 접기 사용법을 추가했습니다.

## 외부 라이브러리를 쓰지 않은 이유

배포 보안 정책(CSP)은 스크립트 출처를 제한하고 있고, 백업 기능이 외부 CDN 상태에 좌우되면 안 됩니다. 그래서 Excel 파일(ZIP + XML)을 만들고 읽는 최소 코드를 직접 넣었습니다. 브라우저 기본 압축 기능(`CompressionStream`)이 있으면 압축해 저장하고, 없으면 압축 없이 저장합니다. Excel이 다시 저장한 파일(공유 문자열 + 압축)도 읽도록 처리했습니다.

## 검증 방법과 결과

| 확인 | 방법 | 결과 |
|---|---|---|
| 파일 구조 | .NET `System.IO.Compression`으로 압축 해제 | 7개 파트 모두 정상 |
| XML 유효성 | .NET `System.Xml`로 전체 파싱 | 7개 파트 모두 통과 |
| **Excel 열기** | Excel COM으로 실제 열기 | **시트 3장 · 내용 정상** |
| 공유 문자열 | Excel이 다시 저장한 형태로 변환해 읽기 | 정상 복원 |
| 값 보존 | 공백·따옴표·줄바꿈·이모지·한자·숫자·null·중첩·10만 자 왕복 | 전부 일치 |
| 실제 복원 | 브라우저에서 내려받은 파일로 복원 요청 | 데이터 25건 · 로그 정상 수신 |
| 휴지통 접기 | 브라우저에서 접기·펼치기·재접속 | 상태 저장·유지 확인 |

## 알아 두실 점

Excel에서 셀을 직접 고치면 복원되지 않을 수 있습니다. 통합 문서는 **확인 용도**로 보시고, 복원에는 내려받은 파일을 그대로 사용해 주세요.

## 실행 명령

~~~bash
npm ci
npm test
~~~

---

## 이전 검증: HLB 로고 색상·서체 정리와 '간편 모드' 배지 제거 (2026-09-10)

- 결과: 자동 테스트 55건 통과, 실패 0건
- 변경 성격: 화면 구성·배치·기능은 그대로 두고 색상과 글꼴만 조정

## 기준 색

첨부된 로고 파일에서 실제 색을 뽑아 기준으로 삼았습니다. 로고는 단색 `#EF4036` 하나만 사용합니다.

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--brand` | `#EF4036` | 로고와 같은 색. 탭 밑줄, 진행 막대처럼 넓게 칠하는 곳 |
| `--brand-strong` | `#C72F27` | 흰 글자를 얹는 버튼과 작은 글자 (대비 확보) |
| `--brand-hover` | `#A9241E` | 버튼을 누르기 직전 상태 |
| `--brand-soft` / `--brand-line` | `#FFF2F0` / `#F3C9C5` | 옅은 배경과 테두리 |

## 수정 내용

1. **'간편 모드' 배지 제거** — 오른쪽 위 배지와 남아 있던 스타일을 정리하고, 브라우저 탭 제목도 `HLB-현장지원팀 CRM`으로 맞췄습니다. 제목을 검사하던 자동 테스트도 함께 고쳤습니다.
2. **파랑 강조색 잔재 정리** — 배지·칩·안내 상자·달력 오늘 표시 등 13곳이 빨간 글자에 파란 테두리처럼 섞여 있었습니다. 모두 브랜드 토큰으로 바꿨습니다. (예: 지원 업무 `진행중` 칩이 빨간 글자 + 파란 테두리였습니다.)
3. **회색을 따뜻한 회색으로** — 화면 곳곳의 차가운 회색 24곳을 로고와 같은 계열의 따뜻한 무채색으로 바꿔 배경·글자·구분선의 색온도를 통일했습니다.
4. **그림자·포커스 링 21곳** — 남색 그림자를 따뜻한 먹색으로, 파란 포커스 링을 브랜드 빨강으로 바꿨습니다.
5. **단계·분류 색 톤 정리** — 파이프라인 단계와 그룹 색이 형광에 가까운 파랑·보라라 브랜드 빨강과 경쟁했습니다. 구분은 유지하면서 채도만 낮춰(`상담 #3B6FA8`, `협상 #7A5B9B`, `리드 #A79E9D`, `제안 #C2762A`) 화면에서 가장 강한 색이 브랜드 빨강이 되도록 했습니다. **마감 단계(수주 초록·보류 주황·실주 빨강)는 뜻이 정해진 색이라 그대로 두었습니다.**
6. **캘린더 범례** — 범례 색 조각이 실제 일정 색과 어긋나 있어 맞췄습니다.
7. **서체** — 로고의 굵고 반듯한 글자에 맞춰 두꺼운 글자가 번지지 않도록 렌더링을 다듬고(`-webkit-font-smoothing`), 숫자를 쓰는 12종류 요소에 고정폭 숫자를 적용해 표·목록에서 세로로 맞아떨어지게 했습니다. 제목 900 / 소제목·탭 800 / 버튼 700의 두께 대비는 그대로 유지했습니다.
8. **스타일시트 정리** — 원본 규칙을 직접 고치면서 뒤쪽 브랜드 블록에서 같은 값을 다시 지정하던 7줄을 제거했습니다.

## 서체를 바꾸지 않은 이유

로고와 가장 비슷한 글꼴로 바꾸려면 새 웹폰트를 받아야 하는데, 이 서버의 보안 정책(CSP)은 `cdn.jsdelivr.net`에서 온 글꼴만 허용합니다. 현재 쓰는 **Pretendard**는 이미 허용된 출처에서 받고 있고 한글 표현이 안정적이며, 굵은 두께에서 로고와 비슷한 반듯한 인상을 냅니다. 그래서 글꼴 종류는 유지하고 두께·자간·숫자 표시만 로고에 맞췄습니다.

## 확인한 화면

대시보드, 파이프라인(목록·보드), 지원 업무, 연락처, 캘린더, 설정, 항목 상세, 담당자 콤보박스, AI 봇, 사용 안내, 관리자 창을 실제 브라우저에서 훑어 파랑·보라 계열이 남아 있는지 자동으로 점검했습니다. 남은 것은 의도적으로 톤을 낮춘 단계 색 3종뿐입니다.

## 기존 사용자에게 남는 부분

파이프라인 **단계 색과 그룹 색은 사용자가 바꿀 수 있어 외부 DB에 저장**됩니다. 이미 한 번이라도 설정에서 저장한 작업공간은 예전 색이 그대로 남습니다. 새 색으로 맞추려면 `설정 > 단계`와 `설정 > 그룹`에서 색을 다시 고르면 됩니다.

`제안`과 `보류`가 같은 주황(`#D97706`)이던 문제는 이번에 `제안`을 `#C2762A`로 낮추며 구분되었습니다.

## 실행 명령

~~~bash
npm ci
npm test
~~~

---

## 이전 검증: 분석 지표 목록·파이프라인과 연락처 저장 버튼 (2026-09-10)

- 최종 `npm test`: **55건 통과, 실패 0건**. 회귀 테스트 6건을 추가하고 기존 자동 저장 테스트를 명시적 저장 흐름에 맞게 갱신했다.
- 대시보드의 수주율·활성 항목 비중·후속조치 지연·실행과제 완료를 버튼으로 변경했다. 계산 대상 목록, 상태·담당자·연락일 또는 마감일, 상세 이동, 빈 목록, HTML 이스케이프, 키보드 닫기와 포커스 이동을 검증했다.
- 수주율 목록은 종료 항목(수주·보류·실주), 실행과제 완료 목록은 전체 과제를 표시하여 기존 비율의 분모와 상태를 함께 확인할 수 있다.
- 파이프라인 상세·연락처 상세에 저장·취소 버튼, 파이프라인 표에 행별 저장 버튼을 추가했다. 신규 입력과 보드 단계 변경도 저장을 눌러야 반영된다. 담당자 다중 선택·대표 변경, 즐겨찾기, 명함 인식·회사명 확인 결과 역시 저장 전에는 임시 입력으로 유지된다.
- 저장 전 API 쓰기 요청이 발생하지 않고 원본 데이터·집계·다른 항목 저장에 섞이지 않음을 검증했다. 취소·닫기 확인, 저장 실패 후 입력 보존과 재시도, 저장 중 닫기 차단, 같은 그룹의 연속 저장, 저장 후 재접속을 검증했다.
- 연락처 저장 후 연결 영업 정보의 갱신 실패를 별도로 알리고 재시도할 수 있다. 명함 이미지 변경 중 연락처 저장이 실패하면 이전 이미지를 복구한다. 관련 테스트를 통과했다.
- 기존 연락처 페이지네이션·리멤버 CSV·담당자 대표 지정·일정 담당자와 연관 업무·활동·지원 업무·API·변경 로그·DB 어댑터 테스트도 통과했다.
- 로컬 Express와 임시 메모리 저장소를 실제 브라우저로 열어 지표 팝업, 상세 이동, 파이프라인·연락처 저장, 새로 연 화면의 저장값 유지와 버튼 가독성을 확인했다. 기존 항목을 열기만 할 때 미저장으로 표시되던 문제도 수정했다.
- 앱 매뉴얼과 README를 갱신하고 JS/CSS 캐시 버전을 `20260910g`로 변경했다. 운영 DB·Render 배포·Google 실계정에 대한 접속·배포 검증은 이번 검증에 포함하지 않았다.

---

## 이전 검증: 대표 지정·일정 담당자·연관 업무 (2026-09-10)

- `npm test`: **49건 통과, 실패 0건** (회귀 테스트 4건 추가).
- 변경한 JavaScript 4개 파일 문법 검사 통과.
- 로컬 Express 서버와 임시 메모리 저장소에서 실제 브라우저로 대표 지정, 일정 담당자 입력, 파이프라인·지원 업무 선택, 저장 및 새로고침 후 재조회 확인.
- 운영 DB·Render 배포·Google 실계정은 변경하거나 접속하지 않음. 아래 과거 검증 결과는 당시 기록임.

### 동작과 검증 범위

1. 선택한 담당자의 `대표로 지정` 버튼으로 대표를 변경한다. 해당 ID를 첫 번째로 저장하므로 기존 대표 표시·연락처 동기화와 호환된다. 새 대표의 자동 입력값, 재접속 후 대표 유지, 대표 해제 시 승계, 직접 수정한 전화번호 보존을 검증했다.
2. 새 일정·일정 수정에 `담당자` 입력을 추가했다. 기존 업무와 일정의 담당자 이름을 추천하며 직접 입력도 가능하다.
3. `연관 업무`에서 파이프라인과 지원 업무를 각각 하나씩 선택할 수 있다. 선택한 업무의 상태·담당자·예정일·다음 할 일을 표시하고, 일정 목록에도 담당자와 연관 업무 제목을 보여 준다.
4. `owner`, `relatedAreaKey`, `relatedDealId`, `relatedTaskId`를 일정에 저장하고 로드 시 보존한다. 입력값 저장·재접속·수정·연결 해제·저장 실패 시 데이터 복구와 재시도를 검증했다. 변경 로그에 읽을 수 있는 필드 이름도 추가했다.
5. 이동한 영업 항목은 유일한 ID로 재연결하고, 찾을 수 없는 원본은 안내를 표시하며 연결값을 보존한다. 악성 HTML이 포함된 제목이 선택 목록과 미리보기에서 실행되지 않음을 검증했다.
6. 종일 일정 체크박스가 일반 입력창 너비로 늘어나던 표시 문제를 수정했다. 실제 브라우저에서 크기와 배치를 확인했다.
7. README와 앱 매뉴얼을 갱신하고 JS/CSS 캐시 버전을 `20260910e`로 변경했다.

---

- 검증일: 2026-08-04
- 대상: `tiniko-crm-cloud`
- 결과: 자동 테스트 29건 통과, 실패 0건
- 의존성 감사: 알려진 취약점 0건

## 검증 범위

| 영역 | 검증 내용 | 결과 |
|---|---|---|
| API | 상태 확인, 연결키 인증, 저장·조회·수정 revision·삭제 | 통과 |
| 저장 키 규칙 | 브라우저의 모든 저장·마이그레이션 키가 서버 승인 `tinico:` 네임스페이스를 사용하는지 | 통과 |
| 관리자 인증 | 잘못된 코드 차단, 단기 세션 토큰, 인증 없는 백업 요청 차단 | 통과 |
| 변경 로그 | 연락처·파이프라인·지원 업무·캘린더·설정의 입력·수정·삭제와 필드 전·후 값 | 통과 |
| 백업·복원 | 전체 데이터와 로그, 초 단위 파일명, 작업공간 검증, 트랜잭션 시점 복원 | 통과 |
| 오류 응답 | 잘못된 JSON 400, 과도한 요청 413, DB 장애 500과 비공개 요청 ID | 통과 |
| 배포 화면 | 정적 파일, SPA 직접 경로, 보안 헤더, HTML 캐시 정책 | 통과 |
| 전체 버튼 연결 | 정적 버튼 107개 런타임 이벤트, 폼 제출, 중복 ID, 삭제된 요소 참조 | 통과 |
| 화면·모달 동작 | 상단·모바일 메뉴, 대시보드 순서, 보드/목록, 도움말, 업로드, AI | 통과 |
| Neon 어댑터 | channel binding 전달, 스키마 SQL, PostgreSQL 레코드 변환 | 통과 |
| 연락처 | 추가, 자동저장, 상세창 닫기, 저장 대기 중 삭제 | 통과 |
| 파이프라인 | 추가, 자동저장, 직접 고객 담당자 이름·연락처 저장, 입력 직후 상세창 닫기, 저장 대기 중 삭제 | 통과 |
| 활동 팝업 | 상세 화면 위 활성화, 취소·저장 중 상세 화면 유지, 활동 저장·삭제 | 통과 |
| 관리자 화면 | 인증 전 버튼 숨김, 코드 팝업, 인증 후 내려받기·복원 버튼 노출 | 통과 |
| 캘린더 | 추가·수정·삭제, 저장 실패 롤백, 버튼 복구, 재시도 | 통과 |
| Google Calendar | 새 일정 1회 생성, 인증 헤더·이벤트 ID 저장, 선택 월 조회·캐시, 수동 새로고침, 수정·삭제 추가 호출 차단, 설정 접기 상태 저장 | 통과 |
| 연결키 | 서버 키 교체 감지, 오래된 키 제거, 새 키 재연결 | 통과 |
| 화면 안전성 | 저장·복원 데이터의 동적 HTML 속성 및 색상값 검증 | 통과 |
| 카메라 수명주기 | 최초 권한 스트림 재사용, 재실행 시 권한 API 미호출, 페이지 종료 시 해제 | 통과 |
| 촬영 품질·즉시 촬영 | 지속형 저조도 경고 팝업, 확인창 제거, 누른 프레임 즉시 저장 | 통과 |
| 명함 OCR | 명암·선명화·그림자·고대비·상하단 영역 인식과 OCR 보안 정책 | 통과 |
| 카메라 예외 복구 | 권한 거부, API 미지원, 영상 준비 시간 초과 시 파일 촬영 전환과 DB 창 비노출 | 통과 |

## 수정한 오류

1. 파이프라인 입력 후 0.45초 안에 상세창을 닫으면 선택 참조가 사라져 `areaKey` 오류가 발생하고 수정값이 저장되지 않던 문제를 수정했습니다.
2. 파이프라인·연락처를 저장 대기 중 삭제하면 남아 있던 타이머가 삭제 뒤 다시 실행되던 경합을 차단했습니다.
3. 파이프라인·연락처 자동저장 실패가 처리되지 않은 Promise 오류가 되던 문제를 수정하고 상세창에 저장 실패 상태를 표시하도록 했습니다.
4. 캘린더 CRM 저장이 실패하면 저장 버튼이 계속 잠기고 메모리 값과 DB 값이 달라지던 문제를 수정했습니다. 마지막 정상값으로 되돌리고 같은 입력을 재시도할 수 있습니다.
5. 캘린더 삭제 중 CRM 저장이 실패할 때 버튼과 화면 상태를 복구하도록 수정했습니다.
6. Render에서 `CRM_ACCESS_KEY`를 바꾼 뒤 브라우저의 오래된 키가 거절되어도 연결 상태가 남던 문제를 수정했습니다.
7. 잘못된 JSON과 요청 크기 초과가 모두 서버 오류 500으로 처리되던 문제를 각각 400과 413으로 구분했습니다.
8. `/pipeline` 같은 직접 접속 경로에서 이전 HTML이 캐시될 수 있던 정책을 모든 화면 경로에서 `no-cache`로 통일했습니다.
9. Neon 연결 문자열의 `channel_binding=require`가 `pg` 옵션에 전달되지 않던 문제를 보완했습니다.
10. 백업 또는 DB에 비정상 식별자·색상값이 들어왔을 때 동적 HTML 속성이 깨질 수 있던 부분을 이스케이프하고 색상값을 제한했습니다.
11. 이미 응답 헤더가 전송된 Express 오류를 다음 오류 처리기로 전달하도록 수정했습니다.
12. 백업·복원 버튼이 일반 사용자에게 항상 노출되던 구조를 서버 검증 기반 관리자 인증으로 변경했습니다. 관리 코드는 브라우저 HTML에 넣지 않고 성공 시 기본 30분짜리 서명 세션만 사용합니다.
13. 기존 백업 파일에 저장 데이터만 있고 어떤 화면에서 무엇이 바뀌었는지 확인할 수 없던 부분을 보완했습니다. 저장 전·후 값을 비교해 화면, 입력·수정·삭제, 항목명, 변경 필드와 전·후 값을 `crm_audit_log`에 기록합니다.
14. 명함 이미지 같은 대용량 문자열이 변경 로그를 불필요하게 키우지 않도록 실제 이미지 대신 길이 표식만 로그에 남깁니다. 복원용 원본 데이터에는 이미지가 그대로 포함됩니다.
15. 여러 저장 키를 차례로 덮어쓰던 복원 방식을 DB 트랜잭션 전체 교체 방식으로 변경했습니다. 복원 도중 오류가 발생하면 기존 데이터와 로그를 유지합니다.
16. 다른 작업공간의 버전 3 백업, 중복 저장 키와 비정상 로그를 복원 전에 거절하도록 검증을 추가했습니다. 기존 단일 HTML의 버전 2 백업은 호환 복원을 지원합니다.
17. 백업 파일명을 `tiniko_crm_backup_년-월-일_시-분-초.json`으로 변경해 날짜뿐 아니라 시·분·초까지 구분할 수 있도록 했습니다.
18. 스캐너를 닫을 때 카메라 트랙을 매번 종료하여 다시 열 때마다 브라우저가 권한을 재확인할 수 있던 문제를 수정했습니다. 같은 페이지에서는 최초 승인 스트림을 비활성 상태로 보존했다가 재사용하고 페이지를 떠날 때만 완전히 해제합니다.
19. 조명 부족·강한 반사·흐림이 상태 문구에만 표시되던 부분을 약 1초간 지속 감지했을 때 화면 위쪽 팝업으로 안내하도록 변경했습니다. 순간적인 밝기 변화는 경고로 확정하지 않습니다.
20. 품질이 나쁠 때 확인 대화상자와 고해상도 정지사진 API를 기다리느라 촬영이 늦어지던 흐름을 제거했습니다. 촬영 버튼을 누른 순간의 프레임을 동기식으로 고정하고 화면을 즉시 닫은 뒤 저장·OCR을 진행합니다.
21. 흐린 촬영 화면을 눌렀을 때 지원 기기에서 단발 또는 연속 초점을 다시 적용하도록 보강하고, 후면 카메라의 이상 해상도를 4096픽셀까지 요청하도록 조정했습니다.
22. 모바일 명함의 작은 글자와 그림자에 대응하도록 명암 확장과 선명화를 강화했습니다. 전체 이미지의 그림자 보정·고대비 인식 후 핵심 필드가 부족하면 상단 이름·회사 영역과 하단 전화·이메일 영역을 겹쳐 추가 인식합니다.
23. 배포 보안 정책에 Tesseract.js CDN, 한국어·영어 OCR 언어 데이터 연결과 웹 워커 실행 출처를 명시하여 Render 환경에서 OCR 리소스가 차단되지 않도록 했습니다.
24. Google Calendar 전체 동기화와 자동 수정·삭제 호출을 제거했습니다. 아직 전송하지 않은 직접 일정은 한 번만 생성하고, 선택한 달의 Google 일정은 한 번 불러온 뒤 재사용하며 사용자가 `Google 일정 불러오기`를 누를 때만 다시 조회하도록 변경했습니다.
25. 이미 Google에 등록된 일정을 CRM에서 수정·삭제할 때 Google API를 다시 호출하지 않도록 검증을 추가했습니다. Google 원본이 유지된다는 안내를 일정 편집·삭제 화면과 내장 매뉴얼에 함께 반영했습니다.
26. 카메라/OCR 및 Google 매뉴얼 마이그레이션 저장 키 두 개가 서버 승인 접두사 `tinico:`가 아닌 `tiniko:`로 작성되어 있었습니다. 실제 Render API는 이를 400으로 거절해 초기화가 중단됐지만 모의 UI 저장소는 허용하던 문제였습니다. 두 키를 수정하고 소스에 `tiniko:` 오타가 하나라도 남으면 실패하는 검증을 추가했습니다.
27. 캘린더에서 제거한 버튼의 `addEventListener` 참조가 남으면 초기화가 중단되고, 실제 원인과 무관하게 외부 DB 오류 창이 표시되던 문제를 수정했습니다. 삭제된 요소 참조, 중복 ID와 모든 정적 버튼의 실제 런타임 이벤트 연결을 자동 검사합니다.
28. 화면 구성 또는 데이터 로딩 예외를 DB 인증 오류로 오인시키던 공통 오류 화면을 분리했습니다. 이제 `UI-BIND` 또는 `UI-DATA` 진단 코드와 `화면 새로고침` 버튼을 표시하고 연결키 입력 폼은 숨깁니다.
29. 카메라 권한 거부·API 미지원·영상 메타데이터 준비 실패가 발생해도 전역 초기화나 DB 인증 상태를 건드리지 않고 휴대폰 기본 촬영·파일 선택으로 전환하도록 보강했습니다. 영상 준비가 6초 이상 끝나지 않으면 스트림을 해제한 뒤 자동 전환합니다.
30. 연락처·활동 기록·지원 업무·단계·그룹 분류·그룹·매뉴얼·휴지통, Google 연결·해제, AI 추가·전송, 대시보드 순서 이동까지 버튼 기반 CRUD와 상태 전환 회귀 테스트를 추가했습니다.
31. Google Calendar 연결 설정이 항상 큰 영역을 차지하던 화면에 `접기/펼치기`를 추가했습니다. 접은 상태를 외부 DB의 앱 설정에 저장하고 저장 실패 시 이전 화면 상태로 되돌리도록 했습니다.
32. 파이프라인 항목 상세에서 직접 입력한 고객 담당자의 전화번호 필드가 화면에 보이지 않던 구조를 수정했습니다. 이름 입력 바로 아래에 전화번호 입력란을 배치하고 연락처 선택 시에도 같은 필드가 갱신되도록 했습니다.
33. `활동 추가` 팝업이 항목 상세 패널보다 낮은 화면 층에 열려 비활성처럼 보이던 문제를 수정했습니다. 팝업을 상세보다 위에 표시하고 취소·저장 전후에도 같은 항목 상세와 배경을 유지합니다.

## 실행 명령

~~~bash
npm ci
npm test
npm audit --audit-level=moderate
~~~

최종 결과는 29개 테스트 모두 통과했고 `npm audit`은 취약점 0건이었습니다.

## 배포 후 필요한 운영 확인

작업 환경에는 실제 `DATABASE_URL`, `CRM_ACCESS_KEY`, Render 서비스 주소, Google 사용자 승인과 물리 휴대폰 카메라가 없으므로 다음 항목은 배포 후 확인해야 합니다.

1. `https://YOUR-SERVICE.onrender.com/api/health`가 `{"status":"ok","database":"connected"}`를 반환하는지 확인합니다.
2. CRM에서 테스트 연락처 하나를 저장하고 새로고침한 뒤 그대로 남아 있는지 확인합니다.
3. 설정의 관리자 코드로 인증한 뒤 백업을 내려받고, 테스트 데이터 수정 후 해당 파일로 복원하여 Neon 데이터가 백업 시점으로 돌아오는지 확인합니다.
4. Google 계정 연결 후 새 직접 일정을 저장하여 Google 기본 캘린더에 한 번 생성되는지 확인합니다. 같은 일정을 CRM에서 다시 수정해도 Google 일정이 중복 생성되지 않는지, CRM에서 삭제해도 Google 원본이 유지되는지 확인합니다.
5. `Google 일정 불러오기`를 눌러 선택한 달의 Google 일정이 초록색으로 표시되는지 확인합니다. 같은 달을 열어 둔 동안 자동 반복 요청이 발생하지 않는지 브라우저 Network에서 확인합니다.
6. 실제 휴대폰에서 카메라를 한 번 허용한 뒤 같은 페이지에서 스캐너를 두 번 이상 열어 권한창이 반복되지 않는지 확인합니다. 브라우저를 재시작한 뒤에는 사이트 권한 설정에 따라 다시 물을 수 있습니다.
7. 밝은 환경·저조도·의도적으로 흐린 환경에서 품질 팝업과 화면 탭 재초점을 확인하고, 서로 다른 한글·영문 명함으로 이름·회사·전화·이메일 인식 결과를 원본과 비교합니다.
8. 최신 Render 배포 후 휴대폰에서 `카메라 스캔`을 눌렀을 때 외부 DB 연결창이 다시 열리지 않는지 확인합니다. `화면 초기화 오류`가 표시되면 연결키 대신 화면의 진단 코드와 같은 시각의 Render 로그를 확인합니다.
9. 캘린더의 Google 간편 동기화를 접은 뒤 새로고침해 접힌 상태가 유지되고, `펼치기`로 다시 설정 항목이 보이는지 확인합니다.
10. 파이프라인 항목에서 고객 담당자 이름과 연락처를 직접 저장한 뒤 `활동 추가`를 열어 팝업이 상세 위에 표시되고, 활동을 저장해도 같은 항목 상세가 열린 채인지 확인합니다.

테스트 출력의 `[csstree-match] BREAK after 15000 iterations`는 큰 단일 HTML의 CSS를 테스트 브라우저가 분석할 때 적용하는 보호 한도 안내이며 테스트 실패나 운영 오류는 아닙니다.

---

# 2차 검증 및 접속 성능 최적화 (2026-09-10)

- 결과: 자동 테스트 41건 통과, 실패 0건
- 변경 성격: 기능 동작은 그대로 두고 접속 시 서버 왕복과 렌더링 비용을 줄임

## 계측 결과

이미 데이터가 저장된 작업공간에 다시 접속할 때(실사용자가 매번 겪는 경로) 발생하던 API 호출입니다.

| 구분 | 이전 | 이후 |
|---|---|---|
| 재접속 시 총 요청 | 38건 (GET 28 · PUT 10) | 28건 (GET 28 · PUT 0) |
| 재접속 시 저장(PUT) 왕복 | 10건 | 0건 |
| 매뉴얼 마이그레이션 표시 조회 | 9건 순차 | 1회 병렬 |
| 재접속 부팅 시간 (왕복 80ms 가정) | 1,958ms | 1,023ms |

## 수정 내용

1. 접속할 때마다 그룹·파이프라인·지원 업무·매뉴얼·연락처를 정규화한 뒤 내용이 같아도 다시 업로드하던 동작을 제거했습니다. PostgreSQL `jsonb`가 객체 키 순서를 보존하지 않아 기존의 `JSON.stringify` 비교가 항상 "변경됨"으로 판정되던 것이 원인이며, 키를 정렬한 표준형(`canonicalJson`)으로 비교하도록 바꿨습니다. 저장 왕복 1건마다 서버에서 행 잠금 트랜잭션과 변경 로그 비교가 함께 돌던 부담도 같이 사라집니다.
2. 매뉴얼 마이그레이션 표시 키 9개를 하나씩 순차 조회하던 것을 한 번에 병렬 조회하고, 새로 기록할 표시도 마지막에 모아서 저장하도록 했습니다.
3. `dealImportance()`가 호출될 때마다 중요도 설정 전체를 깊은 복사하던 것을 캐시로 바꿨습니다. 이 함수는 항목 하나하나, 그리고 목록을 그릴 때마다 호출되므로 항목이 많을수록 효과가 큽니다.
4. 연락처 목록에서 체크박스 하나를 눌러도 표 전체를 다시 만들던 동작을 요약·`모두 선택` 상태만 갱신하도록 바꿨습니다.
5. 사용하지 않는 코드(`backupObject`, `helpButton`)와 중복 호출(`pipeDeals`의 필터 상태 2회 조회)을 정리하고, 그룹별 저장을 순차 대기에서 병렬로 바꿨습니다.
6. 재접속 시 저장 왕복이 다시 생기지 않도록 회귀 테스트(`a repeat visit loads without re-uploading unchanged data`)를 추가했습니다.
7. `app.js` 캐시 버전을 `?v=20260910a`로 올렸습니다.

## 함께 확인한 보안 사항

1. 저장소의 `render.txt`에 CRM 연결키가 평문으로 들어 있었습니다. `.gitignore`에 추가했으나 **파일 삭제와 `CRM_ACCESS_KEY` 교체는 별도로 해야 합니다.**
2. 테스트 파일 두 곳에 운영 기본 관리자 코드가 base64로만 가려진 채 들어 있었습니다. 테스트 전용 코드를 명시적으로 주입하도록 바꾸고, 기본 코드 경로는 "아무 코드나 통과하지 않는다"는 성질로만 검증합니다. 운영에서는 Render의 `CRM_ADMIN_CODE`를 설정해 기본 코드를 교체하는 것을 권장합니다.
3. `npm audit`에서 `qs` 패키지(express 하위 의존성)의 moderate 취약점 2건이 나와 `npm audit fix`로 `6.15.3 → 6.16.0`으로 올렸습니다. 현재 취약점 0건입니다.

## 실행 명령

~~~bash
npm ci
npm test
npm audit --audit-level=moderate
~~~

---

# 메뉴 순서·연락처 페이지 이동·리멤버 CSV 호환 (2026-09-10)

- 결과: 자동 테스트 44건 통과, 실패 0건
- 실제 브라우저(미리보기 서버)에서 화면 동작까지 확인

## 변경 내용

1. 상단·하단 메뉴 순서를 `대시보드 · 파이프라인 · 지원 업무 · 연락처 · 캘린더 · 설정`으로 바꿨습니다.
2. 연락처 목록에 페이지 이동을 추가했습니다. 기본 20줄이며 `«` 첫 페이지, `‹` 이전, 번호 버튼, `›` 다음, `»` 마지막 페이지와 20·30·40·50 드롭다운으로 구성했습니다. 페이지가 10쪽을 넘으면 첫·마지막 쪽을 항상 두고 가운데는 `…`로 줄입니다.
3. 고른 줄 수는 앱 설정(`tinico:app:settings`)에 저장되어 다음 접속에도 유지됩니다. 저장에 실패하면 이전 값으로 되돌리고 알림을 표시합니다.
4. 검색어·정렬을 바꾸면 1페이지로 돌아가고, 삭제 등으로 목록이 줄면 마지막 페이지로 당겨 빈 화면이 나오지 않게 했습니다.
5. 표 머리글의 전체 선택은 지금 보고 있는 페이지만 대상으로 합니다.
6. CSV 업로드에서 이메일·전화가 모두 없는 행(이름과 회사만 있는 명함)도 같은 파일을 다시 올렸을 때 중복되지 않도록 이름+회사로 대조합니다.
7. 설정 > 매뉴얼의 연락처 안내에 페이지 이동과 CSV 사용법을 추가했습니다.

## 리멤버 CSV 호환 확인

실제 리멤버 export 파일(32건)로 업로드 → 내보내기 왕복을 확인했습니다.

| 확인 항목 | 결과 |
|---|---|
| 내보내기 열 구성 | 리멤버 export의 92개 열과 완전히 동일 |
| 업로드 결과 | 32건 전부 등록, 이름·회사 누락 0건 |
| 열 대응 | First Name→이름, Company→회사, Department→부서, Job Title→직함, Mobile Phone→휴대전화, E-mail Address→이메일, User 2→등록일 |
| 등록일 표기 | `2026년 07월 16일`, `2026-07-16` 모두 읽어 `2026-07-16`으로 통일 |
| 해외 번호 | `+81 80-1234-5678`, `+91 98495 13210` 등 국가번호 그대로 보존 |
| 같은 파일 재업로드 | 신규 0명 · 중복 생성 없음 |
| 성/이름 분리 | 리멤버와 같이 `First Name` 한 칸만 사용 |

리멤버 export에는 `User 4`에 명함을 등록한 앱 이름이 들어갑니다. 이 CRM이 내보낼 때는 `HLB-BUSISUP CRM`으로 기록되며, 업로드할 때는 이 값을 사용하지 않으므로 양쪽 호환에 영향이 없습니다.

## 브라우저 실동작 확인

| 동작 | 결과 |
|---|---|
| 연락처 32건 · 기본 20줄 | 20줄 표시 · `1–20 / 전체 32명` |
| `›` 다음 | 12줄 표시 · `21–32 / 전체 32명` · 다음/마지막 버튼 비활성 |
| `«` 첫 페이지 / `»` 마지막 페이지 | 1페이지 / 2페이지로 정상 이동 |
| 드롭다운 50 | 32줄 전체 표시 · 페이지 1개 |
| 드롭다운 30 | 30줄 표시 · 페이지 2개 · 외부 DB에 30 저장 확인 |
| 메뉴 순서 | 대시보드 · 파이프라인 · 지원 업무 · 연락처 · 캘린더 · 설정 |

## 실행 명령

~~~bash
npm ci
npm test
~~~

---

# 고객 담당자 입력 콤보박스 통합 (2026-09-10)

- 결과: 자동 테스트 45건 통과, 실패 0건
- 실제 브라우저(미리보기 서버)에서 입력·필터·선택·자유 입력까지 확인

## 변경 내용

1. 파이프라인 항목 상세의 `고객 담당자 선택`(여러 개 고르는 목록)과 `고객 담당자 직접 입력`(텍스트 칸)을 **`고객 담당자 입력` 한 칸**으로 합치고 콤보박스로 다시 만들었습니다.
2. 텍스트 칸에 입력한 글자와 이름·회사·직함·부서·연락처·이메일이 일치하는 등록 연락처만 아래 목록에 나타납니다. 입력한 글자로 이름이 시작하는 사람을 먼저 보여 주고, 목록은 최대 50명까지 표시합니다.
3. 목록에서 고르면 텍스트 칸이 그 사람의 이름으로 자동 완성되고 직함·연락처·이메일이 함께 채워집니다. 어떤 연락처와 연결됐는지 입력칸 아래에 `연락처 연결됨 · 이름 · 회사 · 직함`으로 표시합니다.
4. 목록에서 고르지 않으면 **입력한 글자를 그대로 저장**합니다. 일치하는 등록 연락처가 없으면 목록 자리에 `입력한 이름 그대로 저장됩니다` 안내가 나옵니다.
5. 연결된 연락처와 다른 이름으로 고쳐 쓰면 연결을 자동으로 풀어 잘못된 연결이 남지 않게 했습니다. 이때 이미 채워진 연락처·이메일 값은 눈에 보이는 입력칸이므로 지우지 않고 그대로 두어 직접 고칠 수 있게 했습니다.
6. 키보드로도 쓸 수 있습니다. `↓`·`↑`로 항목을 옮기고 `Enter`로 고르며, `Esc`로 목록을 닫습니다. 아무것도 고르지 않은 상태의 `Enter`는 입력한 값을 그대로 둡니다. 입력창 오른쪽 `▾`를 누르면 전체 목록이 열립니다.
7. `role="combobox"`, `role="listbox"`, `aria-expanded`, `aria-activedescendant`를 적용해 화면 낭독기에서도 목록 상태를 알 수 있게 했습니다.
8. `고객 담당자 연락처 직접 입력` 이름표는 짝이 되는 `직접 입력` 칸이 없어졌으므로 `고객 담당자 연락처`로 바꿨습니다.
9. 설정 > 매뉴얼의 연락처·파이프라인 안내에서 없어진 두 칸 설명을 지우고 합쳐진 입력 방식을 반영했습니다. 사용자가 직접 고친 다른 문장은 그대로 둡니다.
10. 더 이상 쓰지 않는 `.tn-linked-select` 스타일을 정리했습니다.

## 저장 데이터

`linkedContactIds`는 그대로 두되 목록에서 고른 연락처 한 명만 담습니다(`["연락처ID"]`). 직접 입력한 경우에는 빈 배열입니다. 항목을 열기만 하고 이 칸을 건드리지 않으면 기존 값이 바뀌지 않으므로, 예전에 여러 연락처를 연결해 둔 항목도 그대로 유지됩니다.

## 확인한 동작

| 동작 | 결과 |
|---|---|
| 빈 칸에서 목록 열기 | 등록 연락처 5명 모두 표시 |
| `김` 입력 | 김가온·김나래·김나리 3명만 표시 |
| `김나` 입력 | 김나래·김나리 2명만 표시 |
| `나래바이오`(회사명) 입력 | 김나래 1명만 표시 |
| 목록에서 김나래 선택 | 칸이 `김나래`로 완성 · 연락처 `010-3333-4444` · 직함 `연구소 / 책임` · 이메일 자동 입력 · `연락처 연결됨` 표시 |
| 저장된 값 | `contactName:"김나래"`, `linkedContactIds:["ct-narae"]`, 연락처·이메일·직함 함께 저장 |
| `등록 안 된 김철수 부장` 입력 | 목록에 `입력한 이름 그대로 저장됩니다` 안내 · 연결 해제 · 입력한 글자 그대로 저장 |
| 고르지 않고 `Enter` | 입력한 값 유지 · 목록만 닫힘 |
| `↓` + `Enter` | 첫 항목 선택되어 자동 완성 |

## 실행 명령

~~~bash
npm ci
npm test
~~~

---

# 고객 담당자 다중 선택과 선택 목록 표시 (2026-09-10)

- 결과: 자동 테스트 45건 통과, 실패 0건
- 실제 브라우저(미리보기 서버)에서 추가·해제·대표 승계·직접 입력까지 확인

## 변경 내용

1. `고객 담당자 입력` 콤보박스에서 담당자를 **여러 명** 고를 수 있게 했습니다. 고른 사람은 아래에 쌓이고 검색어 칸은 비워져 다음 사람을 바로 이어서 찾을 수 있습니다.
2. `고객 담당자 연락처` 아래에 **`선택한 담당자`** 목록을 추가했습니다. 한 줄에 `이름`(굵게), `소속 · 직함`, `연락처`를 나란히 보여 주고 오른쪽 `×`로 선택을 해제합니다. 연락처가 없는 담당자는 `연락처 미입력`으로 표시합니다.
3. 이미 고른 사람은 드롭다운 목록에서 빠집니다. 남은 사람이 없으면 `등록된 연락처를 모두 골랐습니다`를 표시합니다.
4. 맨 위 담당자에 `대표` 배지를 붙였습니다. 대표의 직함·연락처·이메일이 위 입력칸에 반영되며, 대표를 해제하면 다음 담당자가 대표가 되면서 연락처도 함께 옮겨집니다. 사용자가 직접 고쳐 둔 값은 덮어쓰지 않고, 이전 대표에서 자동으로 채워졌던 값만 바꿉니다.
5. 한 명이라도 고르면 입력창은 검색 전용이 되고 저장되는 고객 담당자 이름(`contactName`)은 고른 담당자 이름을 모은 값(`김가온, 김나래`)이 됩니다. 목록·검색·AI가 이 값을 그대로 쓰므로 여러 담당자가 모두 검색·표시됩니다.
6. **한 명도 고르지 않으면 입력한 글자를 그대로 저장합니다.** 선택을 모두 해제하면 이름 칸이 비워져 바로 직접 입력으로 돌아갑니다.
7. 연락처 화면에서 담당자의 이름·연락처를 고치면 그 담당자를 연결한 영업 항목에도 반영되도록 `syncLinkedDealsFromContact`를 다중 연결 기준으로 고쳤습니다. 값이 실제로 달라진 항목만 저장해 불필요한 저장 왕복도 없앴습니다.
8. 설정 > 매뉴얼의 연락처·파이프라인 안내에 선택 목록 사용법을 추가했습니다.

## 저장 데이터

| 항목 | 저장 내용 |
|---|---|
| `linkedContactIds` | 고른 순서대로 담당자 ID 배열. 첫 번째가 대표 |
| `contactName` | 고른 담당자가 있으면 이름을 모은 값, 없으면 직접 입력한 글자 |
| `contactRole`·`contactPhone`·`contactEmail` | 대표 담당자 값(직접 고치면 그 값 유지). 고른 담당자가 없으면 직접 입력값 |

## 확인한 동작

| 동작 | 결과 |
|---|---|
| `김` 입력 | 김가온·김나래·김나리 3명만 표시 |
| `나래바이오`(회사명) 입력 | 김나래 1명만 표시 |
| 김가온 선택 | `선택한 담당자`에 `김가온 [대표] · 가온테크 · 영업팀 / 과장 · 010-1111-2222` · 입력창 비워짐 · 연락처 칸 자동 입력 |
| 김나래·이하늘 이어서 선택 | 3줄로 쌓임 · 저장값 `contactName:"김가온, 김나래, 이하늘"` · 대표 연락처는 김가온 유지 |
| 이미 고른 사람 | 드롭다운 목록에서 제외 |
| 연락처 없는 박다솜 선택 | `연락처 미입력`으로 표시 |
| 대표(김가온) 해제 | 김나래가 대표로 승계 · 연락처 칸이 `010-3333-4444`로 변경 · 저장값도 함께 갱신 |
| 전부 해제 | `선택한 담당자가 없습니다` 안내 · 이름·연락처 비워짐 |
| `등록 안 된 최부장` 직접 입력 | 연결 없이 입력한 글자 그대로 저장 |
| `↓` + `Enter` | 키보드로도 추가 |

## 실행 명령

~~~bash
npm ci
npm test
~~~
