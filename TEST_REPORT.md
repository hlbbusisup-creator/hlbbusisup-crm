# HLB-현장지원팀 CRM 오류 검증 및 기능 테스트 보고서

- 검증일: 2026-08-04
- 대상: `tiniko-crm-cloud`
- 결과: 자동 테스트 29건 통과, 실패 0건
- 의존성 감사: 알려진 취약점 0건

## 검증 범위

| 영역 | 검증 내용 | 결과 |
|---|---|---|
| API | 상태 확인, 연결키 인증, 저장·조회·수정 revision·삭제 | 통과 |
| 저장 키 규칙 | 브라우저의 모든 저장·마이그레이션 키가 서버 승인 `tinico:` 네임스페이스를 사용하는지 | 통과 |
| 관리자 인증 | 잘못된 코드 차단, 단기 세션 토큰, 인증 없는 백업 요청 차단 | 통과 |
| 변경 로그 | 연락처·파이프라인·지원 업무·캘린더·설정의 입력·수정·삭제와 필드 전·후 값 | 통과 |
| 백업·복원 | 전체 데이터와 로그, 초 단위 파일명, 작업공간 검증, 트랜잭션 시점 복원 | 통과 |
| 오류 응답 | 잘못된 JSON 400, 과도한 요청 413, DB 장애 500과 비공개 요청 ID | 통과 |
| 배포 화면 | 정적 파일, SPA 직접 경로, 보안 헤더, HTML 캐시 정책 | 통과 |
| 전체 버튼 연결 | 정적 버튼 107개 런타임 이벤트, 폼 제출, 중복 ID, 삭제된 요소 참조 | 통과 |
| 화면·모달 동작 | 상단·모바일 메뉴, 대시보드 순서, 보드/목록, 도움말, 업로드, AI | 통과 |
| Neon 어댑터 | channel binding 전달, 스키마 SQL, PostgreSQL 레코드 변환 | 통과 |
| 연락처 | 추가, 자동저장, 상세창 닫기, 저장 대기 중 삭제 | 통과 |
| 파이프라인 | 추가, 자동저장, 직접 고객 담당자 이름·연락처 저장, 입력 직후 상세창 닫기, 저장 대기 중 삭제 | 통과 |
| 활동 팝업 | 상세 화면 위 활성화, 취소·저장 중 상세 화면 유지, 활동 저장·삭제 | 통과 |
| 관리자 화면 | 인증 전 버튼 숨김, 코드 팝업, 인증 후 내려받기·복원 버튼 노출 | 통과 |
| 캘린더 | 추가·수정·삭제, 저장 실패 롤백, 버튼 복구, 재시도 | 통과 |
| Google Calendar | 새 일정 1회 생성, 인증 헤더·이벤트 ID 저장, 선택 월 조회·캐시, 수동 새로고침, 수정·삭제 추가 호출 차단, 설정 접기 상태 저장 | 통과 |
| 연결키 | 서버 키 교체 감지, 오래된 키 제거, 새 키 재연결 | 통과 |
| 화면 안전성 | 저장·복원 데이터의 동적 HTML 속성 및 색상값 검증 | 통과 |
| 카메라 수명주기 | 최초 권한 스트림 재사용, 재실행 시 권한 API 미호출, 페이지 종료 시 해제 | 통과 |
| 촬영 품질·즉시 촬영 | 지속형 저조도 경고 팝업, 확인창 제거, 누른 프레임 즉시 저장 | 통과 |
| 명함 OCR | 명암·선명화·그림자·고대비·상하단 영역 인식과 OCR 보안 정책 | 통과 |
| 카메라 예외 복구 | 권한 거부, API 미지원, 영상 준비 시간 초과 시 파일 촬영 전환과 DB 창 비노출 | 통과 |

## 수정한 오류

1. 파이프라인 입력 후 0.45초 안에 상세창을 닫으면 선택 참조가 사라져 `areaKey` 오류가 발생하고 수정값이 저장되지 않던 문제를 수정했습니다.
2. 파이프라인·연락처를 저장 대기 중 삭제하면 남아 있던 타이머가 삭제 뒤 다시 실행되던 경합을 차단했습니다.
3. 파이프라인·연락처 자동저장 실패가 처리되지 않은 Promise 오류가 되던 문제를 수정하고 상세창에 저장 실패 상태를 표시하도록 했습니다.
4. 캘린더 CRM 저장이 실패하면 저장 버튼이 계속 잠기고 메모리 값과 DB 값이 달라지던 문제를 수정했습니다. 마지막 정상값으로 되돌리고 같은 입력을 재시도할 수 있습니다.
5. 캘린더 삭제 중 CRM 저장이 실패할 때 버튼과 화면 상태를 복구하도록 수정했습니다.
6. Render에서 `CRM_ACCESS_KEY`를 바꾼 뒤 브라우저의 오래된 키가 거절되어도 연결 상태가 남던 문제를 수정했습니다.
7. 잘못된 JSON과 요청 크기 초과가 모두 서버 오류 500으로 처리되던 문제를 각각 400과 413으로 구분했습니다.
8. `/pipeline` 같은 직접 접속 경로에서 이전 HTML이 캐시될 수 있던 정책을 모든 화면 경로에서 `no-cache`로 통일했습니다.
9. Neon 연결 문자열의 `channel_binding=require`가 `pg` 옵션에 전달되지 않던 문제를 보완했습니다.
10. 백업 또는 DB에 비정상 식별자·색상값이 들어왔을 때 동적 HTML 속성이 깨질 수 있던 부분을 이스케이프하고 색상값을 제한했습니다.
11. 이미 응답 헤더가 전송된 Express 오류를 다음 오류 처리기로 전달하도록 수정했습니다.
12. 백업·복원 버튼이 일반 사용자에게 항상 노출되던 구조를 서버 검증 기반 관리자 인증으로 변경했습니다. 관리 코드는 브라우저 HTML에 넣지 않고 성공 시 기본 30분짜리 서명 세션만 사용합니다.
13. 기존 백업 파일에 저장 데이터만 있고 어떤 화면에서 무엇이 바뀌었는지 확인할 수 없던 부분을 보완했습니다. 저장 전·후 값을 비교해 화면, 입력·수정·삭제, 항목명, 변경 필드와 전·후 값을 `crm_audit_log`에 기록합니다.
14. 명함 이미지 같은 대용량 문자열이 변경 로그를 불필요하게 키우지 않도록 실제 이미지 대신 길이 표식만 로그에 남깁니다. 복원용 원본 데이터에는 이미지가 그대로 포함됩니다.
15. 여러 저장 키를 차례로 덮어쓰던 복원 방식을 DB 트랜잭션 전체 교체 방식으로 변경했습니다. 복원 도중 오류가 발생하면 기존 데이터와 로그를 유지합니다.
16. 다른 작업공간의 버전 3 백업, 중복 저장 키와 비정상 로그를 복원 전에 거절하도록 검증을 추가했습니다. 기존 단일 HTML의 버전 2 백업은 호환 복원을 지원합니다.
17. 백업 파일명을 `tiniko_crm_backup_년-월-일_시-분-초.json`으로 변경해 날짜뿐 아니라 시·분·초까지 구분할 수 있도록 했습니다.
18. 스캐너를 닫을 때 카메라 트랙을 매번 종료하여 다시 열 때마다 브라우저가 권한을 재확인할 수 있던 문제를 수정했습니다. 같은 페이지에서는 최초 승인 스트림을 비활성 상태로 보존했다가 재사용하고 페이지를 떠날 때만 완전히 해제합니다.
19. 조명 부족·강한 반사·흐림이 상태 문구에만 표시되던 부분을 약 1초간 지속 감지했을 때 화면 위쪽 팝업으로 안내하도록 변경했습니다. 순간적인 밝기 변화는 경고로 확정하지 않습니다.
20. 품질이 나쁠 때 확인 대화상자와 고해상도 정지사진 API를 기다리느라 촬영이 늦어지던 흐름을 제거했습니다. 촬영 버튼을 누른 순간의 프레임을 동기식으로 고정하고 화면을 즉시 닫은 뒤 저장·OCR을 진행합니다.
21. 흐린 촬영 화면을 눌렀을 때 지원 기기에서 단발 또는 연속 초점을 다시 적용하도록 보강하고, 후면 카메라의 이상 해상도를 4096픽셀까지 요청하도록 조정했습니다.
22. 모바일 명함의 작은 글자와 그림자에 대응하도록 명암 확장과 선명화를 강화했습니다. 전체 이미지의 그림자 보정·고대비 인식 후 핵심 필드가 부족하면 상단 이름·회사 영역과 하단 전화·이메일 영역을 겹쳐 추가 인식합니다.
23. 배포 보안 정책에 Tesseract.js CDN, 한국어·영어 OCR 언어 데이터 연결과 웹 워커 실행 출처를 명시하여 Render 환경에서 OCR 리소스가 차단되지 않도록 했습니다.
24. Google Calendar 전체 동기화와 자동 수정·삭제 호출을 제거했습니다. 아직 전송하지 않은 직접 일정은 한 번만 생성하고, 선택한 달의 Google 일정은 한 번 불러온 뒤 재사용하며 사용자가 `Google 일정 불러오기`를 누를 때만 다시 조회하도록 변경했습니다.
25. 이미 Google에 등록된 일정을 CRM에서 수정·삭제할 때 Google API를 다시 호출하지 않도록 검증을 추가했습니다. Google 원본이 유지된다는 안내를 일정 편집·삭제 화면과 내장 매뉴얼에 함께 반영했습니다.
26. 카메라/OCR 및 Google 매뉴얼 마이그레이션 저장 키 두 개가 서버 승인 접두사 `tinico:`가 아닌 `tiniko:`로 작성되어 있었습니다. 실제 Render API는 이를 400으로 거절해 초기화가 중단됐지만 모의 UI 저장소는 허용하던 문제였습니다. 두 키를 수정하고 소스에 `tiniko:` 오타가 하나라도 남으면 실패하는 검증을 추가했습니다.
27. 캘린더에서 제거한 버튼의 `addEventListener` 참조가 남으면 초기화가 중단되고, 실제 원인과 무관하게 외부 DB 오류 창이 표시되던 문제를 수정했습니다. 삭제된 요소 참조, 중복 ID와 모든 정적 버튼의 실제 런타임 이벤트 연결을 자동 검사합니다.
28. 화면 구성 또는 데이터 로딩 예외를 DB 인증 오류로 오인시키던 공통 오류 화면을 분리했습니다. 이제 `UI-BIND` 또는 `UI-DATA` 진단 코드와 `화면 새로고침` 버튼을 표시하고 연결키 입력 폼은 숨깁니다.
29. 카메라 권한 거부·API 미지원·영상 메타데이터 준비 실패가 발생해도 전역 초기화나 DB 인증 상태를 건드리지 않고 휴대폰 기본 촬영·파일 선택으로 전환하도록 보강했습니다. 영상 준비가 6초 이상 끝나지 않으면 스트림을 해제한 뒤 자동 전환합니다.
30. 연락처·활동 기록·지원 업무·단계·그룹 분류·그룹·매뉴얼·휴지통, Google 연결·해제, AI 추가·전송, 대시보드 순서 이동까지 버튼 기반 CRUD와 상태 전환 회귀 테스트를 추가했습니다.
31. Google Calendar 연결 설정이 항상 큰 영역을 차지하던 화면에 `접기/펼치기`를 추가했습니다. 접은 상태를 외부 DB의 앱 설정에 저장하고 저장 실패 시 이전 화면 상태로 되돌리도록 했습니다.
32. 파이프라인 항목 상세에서 직접 입력한 고객 담당자의 전화번호 필드가 화면에 보이지 않던 구조를 수정했습니다. 이름 입력 바로 아래에 전화번호 입력란을 배치하고 연락처 선택 시에도 같은 필드가 갱신되도록 했습니다.
33. `활동 추가` 팝업이 항목 상세 패널보다 낮은 화면 층에 열려 비활성처럼 보이던 문제를 수정했습니다. 팝업을 상세보다 위에 표시하고 취소·저장 전후에도 같은 항목 상세와 배경을 유지합니다.

## 실행 명령

~~~bash
npm ci
npm test
npm audit --audit-level=moderate
~~~

최종 결과는 29개 테스트 모두 통과했고 `npm audit`은 취약점 0건이었습니다.

## 배포 후 필요한 운영 확인

작업 환경에는 실제 `DATABASE_URL`, `CRM_ACCESS_KEY`, Render 서비스 주소, Google 사용자 승인과 물리 휴대폰 카메라가 없으므로 다음 항목은 배포 후 확인해야 합니다.

1. `https://YOUR-SERVICE.onrender.com/api/health`가 `{"status":"ok","database":"connected"}`를 반환하는지 확인합니다.
2. CRM에서 테스트 연락처 하나를 저장하고 새로고침한 뒤 그대로 남아 있는지 확인합니다.
3. 설정의 관리자 코드로 인증한 뒤 백업을 내려받고, 테스트 데이터 수정 후 해당 파일로 복원하여 Neon 데이터가 백업 시점으로 돌아오는지 확인합니다.
4. Google 계정 연결 후 새 직접 일정을 저장하여 Google 기본 캘린더에 한 번 생성되는지 확인합니다. 같은 일정을 CRM에서 다시 수정해도 Google 일정이 중복 생성되지 않는지, CRM에서 삭제해도 Google 원본이 유지되는지 확인합니다.
5. `Google 일정 불러오기`를 눌러 선택한 달의 Google 일정이 초록색으로 표시되는지 확인합니다. 같은 달을 열어 둔 동안 자동 반복 요청이 발생하지 않는지 브라우저 Network에서 확인합니다.
6. 실제 휴대폰에서 카메라를 한 번 허용한 뒤 같은 페이지에서 스캐너를 두 번 이상 열어 권한창이 반복되지 않는지 확인합니다. 브라우저를 재시작한 뒤에는 사이트 권한 설정에 따라 다시 물을 수 있습니다.
7. 밝은 환경·저조도·의도적으로 흐린 환경에서 품질 팝업과 화면 탭 재초점을 확인하고, 서로 다른 한글·영문 명함으로 이름·회사·전화·이메일 인식 결과를 원본과 비교합니다.
8. 최신 Render 배포 후 휴대폰에서 `카메라 스캔`을 눌렀을 때 외부 DB 연결창이 다시 열리지 않는지 확인합니다. `화면 초기화 오류`가 표시되면 연결키 대신 화면의 진단 코드와 같은 시각의 Render 로그를 확인합니다.
9. 캘린더의 Google 간편 동기화를 접은 뒤 새로고침해 접힌 상태가 유지되고, `펼치기`로 다시 설정 항목이 보이는지 확인합니다.
10. 파이프라인 항목에서 고객 담당자 이름과 연락처를 직접 저장한 뒤 `활동 추가`를 열어 팝업이 상세 위에 표시되고, 활동을 저장해도 같은 항목 상세가 열린 채인지 확인합니다.

테스트 출력의 `[csstree-match] BREAK after 15000 iterations`는 큰 단일 HTML의 CSS를 테스트 브라우저가 분석할 때 적용하는 보호 한도 안내이며 테스트 실패나 운영 오류는 아닙니다.
